interface:
  display_name: "Archeo Skill"
  short_description: "Repo-specific patterns and workflows for archeo-skill"
  default_prompt: "Use the archeo-skill repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true